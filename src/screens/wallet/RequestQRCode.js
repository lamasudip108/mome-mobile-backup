import React from 'react';

import RequestQRCode from './components/RequestQRCode';

const RequestQRCodeScreen = (props) => {

    return (
        <RequestQRCode
            {...props}
        />
    );

};

export default RequestQRCodeScreen;
